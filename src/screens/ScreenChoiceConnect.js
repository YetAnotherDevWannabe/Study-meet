import { Button, Text, View } from "react-native";

export default function ScreenChoiceConnect({route, navigation})
{
	console.log("Route", route)

	return (
		<View style={{flex:1, backgroundColor:'#FFF'}}>
			<Text>ScreenChoiceConnect</Text>
		</View>
	)
}